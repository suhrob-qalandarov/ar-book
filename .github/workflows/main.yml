name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Verify Maven build output
        run: |
          ls -la target/
          if [ ! -f target/ar-book-0.0.1-SNAPSHOT.jar ]; then
            echo "Error: ar-book-0.0.1-SNAPSHOT.jar not found in target directory"
            exit 1
          fi

      - name: Copy files to server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          source: "Dockerfile,docker-compose.yml,pom.xml,src/**,target/ar-book-0.0.1-SNAPSHOT.jar"
          target: "/tmp/ar-book-deployment"

      - name: Login to Server via SSH Password
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          timeout: 30s
          command_timeout: 10m
          script: |
            # Ensure target directory exists
            mkdir -p /home/${{ secrets.SSH_USER }}/ar-book || exit 1

            # Remove all old files to avoid overwrite errors
            rm -rf /home/${{ secrets.SSH_USER }}/ar-book/* || exit 1

            # Move new deployment files
            mv /tmp/ar-book-deployment/* /home/${{ secrets.SSH_USER }}/ar-book/ || exit 1
            cd /home/${{ secrets.SSH_USER }}/ar-book || exit 1

            # Verify JAR file presence
            if [ ! -f ar-book-0.0.1-SNAPSHOT.jar ]; then
              echo "Error: ar-book-0.0.1-SNAPSHOT.jar not found in /home/${{ secrets.SSH_USER }}/ar-book"
              exit 1
            fi

            # Create .env file from secrets
            cat <<EOF > .env
            DB_URL=${{ secrets.DB_URL }}
            DB_USER=${{ secrets.DB_USER }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            BUCKET_NAME=${{ secrets.BUCKET_NAME }}
            BUCKET_REGION=${{ secrets.BUCKET_REGION }}
            S3_ACCESS_KEY=${{ secrets.S3_ACCESS_KEY }}
            S3_SECRET_KEY=${{ secrets.S3_SECRET_KEY }}
            JWT_EXPIRATION=${{ secrets.JWT_EXPIRATION }}
            JWT_SECRET_KEY=${{ secrets.JWT_SECRET_KEY }}
            EOF

            docker-compose down --remove-orphans || exit 1
            docker image prune -f || exit 1
            docker-compose build || exit 1
            docker-compose up -d || exit 1